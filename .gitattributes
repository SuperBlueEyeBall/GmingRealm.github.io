gfiles/*.md binary
gfiles/*).md binary

# The stuff above is so it doesn't give a "UTF-8" encoding error as it uses a custom file reader for some games apparently

# --------------------------------------------------------------------------------------------------------------------------------

# *.unityweb filter=lfs diff=lfs merge=lfs -text
# This is a comment. Just delete the "# " to add git LFS back.
# I do not recommend adding git LFS as it turns a file into
# text pointing to the actual file. It's only useful
# for if you want to store something to download later.
# You can't use LFS for GitHub pages so yeah, pretty dissapointing.
